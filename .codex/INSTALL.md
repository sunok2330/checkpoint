# Installing Checkpoint for Codex CLI

1. Copy `codex/checkpoint/SKILL.md` to your project
2. Add the Anti-Amnesia Protocol to your project instructions
3. Create a `memory/` directory with a `MEMORY.md` index file

The skill will be available when you reference it in your Codex CLI conversations.
